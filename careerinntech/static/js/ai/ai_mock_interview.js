document.getElementById("startInterviewBtn").addEventListener("click", () => {
    alert(
        "AI Mock Interview session will start next.\n\n" +
        "Mic permission and interview setup will be handled in the next step."
    );

    // Later this will redirect to live interview session
    // window.location.href = "/ai/interview-session/";
});
